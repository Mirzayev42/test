spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bankdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 'rasif1103!'
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.example.BankSystem: DEBUG

